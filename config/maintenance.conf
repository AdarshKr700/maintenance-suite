# Maintenance suite configuration (do not commit secrets here)
# Paths
BACKUP_SRC="/etc /home"
BACKUP_DEST="./var_backups"
LOGFILE="./logs/maintenance-suite.log"

# Behavior
KEEP_DAYS=14
RETENTION_DAYS=30

# Alerts
ALERT_EMAIL=""   # set to an email if using mailx / mailutils
